{
  "name": "local-sqs",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "build": "rm -rf dist/* && tsc",
    "start": "node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "debug": "tsc && ts-node-dev  --inspect --respawn --transpile-only ./index.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.8",
    "aws-sdk": "^2.729.0",
    "ts-node": "^8.10.2",
    "ts-node-dev": "^1.0.0-pre.60",
    "tslint": "^6.1.3",
    "typescript": "^3.9.7"

  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
